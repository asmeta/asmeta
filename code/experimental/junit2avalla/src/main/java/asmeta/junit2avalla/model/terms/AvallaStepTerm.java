package asmeta.junit2avalla.model.terms;

/**
 * Represents a step term in the Avalla framework,
 * which triggers a step in the ASM (Abstract State Machine).
 * <p>
 * A step in Avalla is a command that instructs the ASM to perform a single computational step,
 * progressing the state of the machine according to its defined rules and behaviors.
 * </p>
 */
public class AvallaStepTerm extends AvallaTerm {

}

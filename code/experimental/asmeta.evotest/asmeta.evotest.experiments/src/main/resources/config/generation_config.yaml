# --- Input specification ---
input:
  mode: "single"   # one of: single | directory | list
  # --- single file mode ---
  source_file: "data/icst-26-exp/Temp.asm"
  # --- directory mode ---
  source_dir: "data/ase-exp/models/"
  # --- list mode ---
  model_list_file: "data/icst-26-exp/model_list.txt"
  base_dir: "../../../../asm_examples/"

# --- Output configuration ---
output:
  target_folder: "data/icst-26-exp/results/"

# --- Algorithms to run ---
algorithms:
  random:
    run: false
    shuffle: false
    step_list: [20]
  atgt:
    run: false
  evoavalla:
    run: true
    shuffle: true
    jdk_path: "C:/Program Files/Java/jdk1.8.0_202"
    budget: 5
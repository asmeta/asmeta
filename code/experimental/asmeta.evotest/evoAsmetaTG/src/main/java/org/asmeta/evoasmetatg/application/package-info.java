/**
 * The application package is responsible for exposing the methods to interact
 * with the application from the outside, this is the task of the
 * {@link Translator} interface, which is implemented by the
 * {@link TranslatorImpl} class. The {@link TranslatorConstants} class contains
 * all the constants used by the Translator in this package. There is also the
 * custom exception {@link TranslationException} that handles the errors that
 * occur during the translation process.
 */
package org.asmeta.evoasmetatg.application;

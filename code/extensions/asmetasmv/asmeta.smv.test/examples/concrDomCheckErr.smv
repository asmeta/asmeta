MODULE main
	VAR
		cond: boolean;
		foo: 1..4;
	ASSIGN
		init(cond) := TRUE;
		init(foo) := 1;
		next(cond):= !(cond);
		next(foo):=
			case
				!(cond): (foo - 1);
				cond: (foo + 1);
				TRUE: foo;
			esac;
